<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CHISPA — Sistema de Gestión de Vida</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg: #000000;
      --panel: #0b0b0b;
      --line: rgba(255,255,255,0.08);
      --soft: rgba(255,255,255,0.6);
      --text: rgba(255,255,255,0.95);
      --accent: #8df0c8;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Cormorant Garamond', serif;
      overflow: hidden;
      height: 100vh;
      width: 100vw;
    }

    .app {
      position: relative;
      width: 100vw;
      height: 100vh;
      overflow: hidden;
    }

    .screen {
      position: absolute;
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px;
      transition: transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
    }

    .panel {
      width: 90%;
      max-width: 420px;
      min-height: 72vh;
      background: linear-gradient(
        180deg,
        rgba(255,255,255,0.03),
        rgba(255,255,255,0.01)
      );
      border: 1px solid var(--line);
      border-radius: 32px;
      backdrop-filter: blur(20px);
      padding: 42px 34px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      box-shadow:
        0 0 40px rgba(255,255,255,0.03),
        inset 0 0 20px rgba(255,255,255,0.02);
    }

    .direction {
      font-size: 12px;
      letter-spacing: 6px;
      color: var(--soft);
      text-transform: uppercase;
      margin-bottom: 10px;
    }

    h1 {
      font-size: 52px;
      font-weight: 300;
      letter-spacing: 2px;
      margin-bottom: 10px;
    }

    .subtitle {
      font-size: 18px;
      color: var(--soft);
      line-height: 1.5;
      max-width: 260px;
    }

    .center-symbol {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 30px 0;
    }

    .circle {
      width: 170px;
      height: 170px;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 50%;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .circle::before,
    .circle::after {
      content: '';
      position: absolute;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.06);
    }

    .circle::before {
      width: 120px;
      height: 120px;
    }

    .circle::after {
      width: 70px;
      height: 70px;
    }

    .plus {
      color: var(--accent);
      font-size: 28px;
      font-weight: 300;
    }

    .widget {
      border-top: 1px solid var(--line);
      padding-top: 22px;
      margin-top: 20px;
    }

    .widget-label {
      font-size: 12px;
      letter-spacing: 4px;
      color: var(--soft);
      margin-bottom: 10px;
      text-transform: uppercase;
    }

    .focus-title {
      font-size: 30px;
      margin-bottom: 12px;
      font-weight: 400;
    }

    .progress {
      width: 100%;
      height: 2px;
      background: rgba(255,255,255,0.08);
      overflow: hidden;
      margin: 20px 0;
    }

    .progress-bar {
      width: 62%;
      height: 100%;
      background: var(--accent);
    }

    .metrics {
      display: flex;
      justify-content: space-between;
      color: var(--soft);
      font-size: 18px;
    }

    .quote {
      color: rgba(255,255,255,0.5);
      line-height: 1.7;
      font-size: 18px;
      max-width: 280px;
    }

    .navigation-hint {
      position: absolute;
      bottom: 30px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 12px;
      letter-spacing: 4px;
      color: rgba(255,255,255,0.35);
      text-transform: uppercase;
    }

    .north {
      transform: translateY(-100%);
    }

    .south {
      transform: translateY(100%);
    }

    .east {
      transform: translateX(100%);
    }

    .west {
      transform: translateX(-100%);
    }

    .center {
      transform: translate(0,0);
    }

    .active {
      transform: translate(0,0);
      z-index: 10;
    }

    .hidden-up {
      transform: translateY(-100%);
    }

    .hidden-down {
      transform: translateY(100%);
    }

    .hidden-left {
      transform: translateX(-100%);
    }

    .hidden-right {
      transform: translateX(100%);
    }

    .minimal-list {
      display: flex;
      flex-direction: column;
      gap: 18px;
      margin-top: 20px;
    }

    .minimal-item {
      padding-bottom: 12px;
      border-bottom: 1px solid rgba(255,255,255,0.05);
      color: rgba(255,255,255,0.82);
      font-size: 22px;
    }

    @media (max-width: 600px) {
      .panel {
        width: 100%;
        min-height: 100%;
        border-radius: 0;
      }

      h1 {
        font-size: 42px;
      }
    }
  </style>
</head>
<body>

  <div class="app">

    <!-- CENTER -->
    <section class="screen center active" id="center">
      <div class="panel">

        <div>
          <div class="direction">[ C ]</div>
          <h1>CHISPA</h1>
          <p class="subtitle">
            El Faro del Ahora.
            Navegación consciente de tu existencia.
          </p>
        </div>

        <div class="center-symbol">
          <div class="circle">
            <div class="plus">+</div>
          </div>
        </div>

        <div class="widget">
          <div class="widget-label">Enfoque Actual</div>
          <div class="focus-title">Escribiendo Capítulo 7</div>

          <div class="progress">
            <div class="progress-bar"></div>
          </div>

          <div class="metrics">
            <span>62%</span>
            <span>1h 42m</span>
          </div>
        </div>

        <p class="quote">
          “Donde está tu atención,
          está tu vida.”
        </p>

      </div>

      <div class="navigation-hint">
        Swipe / Flechas
      </div>
    </section>

    <!-- NORTH -->
    <section class="screen north" id="north">
      <div class="panel">
        <div>
          <div class="direction">[ N ]</div>
          <h1>NORTE</h1>
          <p class="subtitle">
            Brújula espiritual y propósito.
          </p>
        </div>

        <div class="minimal-list">
          <div class="minimal-item">Meditación</div>
          <div class="minimal-item">Misión de Vida</div>
          <div class="minimal-item">Journaling</div>
        </div>

        <p class="quote">
          “El alma necesita dirección,
          no velocidad.”
        </p>
      </div>
    </section>

    <!-- SOUTH -->
    <section class="screen south" id="south">
      <div class="panel">
        <div>
          <div class="direction">[ S ]</div>
          <h1>SUR</h1>
          <p class="subtitle">
            El templo físico que sostiene todo.
          </p>
        </div>

        <div class="minimal-list">
          <div class="minimal-item">Muay Thai</div>
          <div class="minimal-item">Cold Shower</div>
          <div class="minimal-item">Recovery</div>
        </div>

        <p class="quote">
          “Disciplina física,
          claridad mental.”
        </p>
      </div>
    </section>

    <!-- EAST -->
    <section class="screen east" id="east">
      <div class="panel">
        <div>
          <div class="direction">[ E ]</div>
          <h1>ESTE</h1>
          <p class="subtitle">
            Amanecer creativo y expansión.
          </p>
        </div>

        <div class="minimal-list">
          <div class="minimal-item">Novel Writing</div>
          <div class="minimal-item">Alexandria Tech</div>
          <div class="minimal-item">Anderson Matos LLC</div>
        </div>

        <p class="quote">
          “Crear también es
          una forma de rezar.”
        </p>
      </div>
    </section>

    <!-- WEST -->
    <section class="screen west" id="west">
      <div class="panel">
        <div>
          <div class="direction">[ O ]</div>
          <h1>OESTE</h1>
          <p class="subtitle">
            Responsabilidad y servicio externo.
          </p>
        </div>

        <div class="minimal-list">
          <div class="minimal-item">CNA Shift</div>
          <div class="minimal-item">Red Cross</div>
          <div class="minimal-item">Community</div>
        </div>

        <p class="quote">
          “Servir también es
          construir significado.”
        </p>
      </div>
    </section>

  </div>

  <script>
    const screens = {
      center: document.getElementById('center'),
      north: document.getElementById('north'),
      south: document.getElementById('south'),
      east: document.getElementById('east'),
      west: document.getElementById('west')
    };

    let current = 'center';

    function resetPositions() {
      screens.center.className = 'screen center';
      screens.north.className = 'screen north';
      screens.south.className = 'screen south';
      screens.east.className = 'screen east';
      screens.west.className = 'screen west';
    }

    function navigate(direction) {
      resetPositions();

      if (direction === 'up') {
        screens.north.classList.add('active');
        current = 'north';
      }

      else if (direction === 'down') {
        screens.south.classList.add('active');
        current = 'south';
      }

      else if (direction === 'left') {
        screens.west.classList.add('active');
        current = 'west';
      }

      else if (direction === 'right') {
        screens.east.classList.add('active');
        current = 'east';
      }

      else {
        screens.center.classList.add('active');
        current = 'center';
      }
    }

    document.addEventListener('keydown', (e) => {
      if (current !== 'center') {
        navigate('center');
        return;
      }

      switch(e.key) {
        case 'ArrowUp':
          navigate('up');
          break;

        case 'ArrowDown':
          navigate('down');
          break;

        case 'ArrowLeft':
          navigate('left');
          break;

        case 'ArrowRight':
          navigate('right');
          break;
      }
    });

    let touchStartX = 0;
    let touchStartY = 0;

    document.addEventListener('touchstart', e => {
      touchStartX = e.changedTouches[0].screenX;
      touchStartY = e.changedTouches[0].screenY;
    });

    document.addEventListener('touchend', e => {
      const touchEndX = e.changedTouches[0].screenX;
      const touchEndY = e.changedTouches[0].screenY;

      const dx = touchEndX - touchStartX;
      const dy = touchEndY - touchStartY;

      if (current !== 'center') {
        navigate('center');
        return;
      }

      if (Math.abs(dx) > Math.abs(dy)) {
        if (dx > 50) navigate('right');
        else if (dx < -50) navigate('left');
      } else {
        if (dy > 50) navigate('down');
        else if (dy < -50) navigate('up');
      }
    });
  </script>

</body>
</html>
